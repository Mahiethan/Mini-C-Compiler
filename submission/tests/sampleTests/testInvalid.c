int addition(int n, int m){
	&int result; //& here is a valid symbol, it just doesn't follow grammar - won't be detected by lexical analyser
    result = #3 + 4 * -!m; //# here is not a valid symbol
}