// #include <stdbool.h>
// float a;
extern int ext(int a,int b);
void test()
{
    //bool a;
    int a;
    // a = 5 + 3 / -3 * a;
    a = 100 + 4 / -5 * (5 + 4) % ext(4,5);
    //a = true + 4 / -5 * (5 + 4) % ext(4,5,3,1,2,(2&&2/0));


}
// a = 2 == true + 5.0 + 4.5 * 19;
// bool a = True | True;
int another()
{
    float b;
    b = s(a,b,c,d,2);

    {
        a = 1.0;
        b = 2.32;
        c = false;
    }

    //acts as && for while loop conditions
    while(b==2) 
    while(c==1) 
    while(d==2)
    {
        //cant have multiple if stmts like this
        // if(a == 1)
        // if(c==1) 
        if(d==2)
        {
            of = 5*7;
        }
        // not valid
        // else if(b == 2)
        // {

        // }
        else
        {
            asds = 5;
        }

       

        return 5;

        a = 5;
    }
}
